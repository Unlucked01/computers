# ===========================================
# ПРОДАКШН ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ
# ===========================================
# ВАЖНО: Измените все значения на безопасные перед деплоем!

# ===========================================
# БАЗА ДАННЫХ (внешний PostgreSQL контейнер)
# ===========================================
DATABASE_HOST=host.docker.internal
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=your_secure_postgres_password_here
DATABASE_NAME=pc_configurator

# ===========================================
# БЕЗОПАСНОСТЬ
# ===========================================
# JWT секрет (сгенерируйте безопасный ключ минимум 32 символа)
# Команда для генерации: openssl rand -hex 32
JWT_SECRET_KEY=your_very_secure_jwt_secret_key_here_at_least_32_characters_long

# ===========================================
# SSL СЕРТИФИКАТЫ (Let's Encrypt)
# ===========================================
LETSENCRYPT_EMAIL=your-email@example.com

# ===========================================
# ДОМЕН
# ===========================================
DOMAIN=unl-computers.duckdns.org

# ===========================================
# API URL ДЛЯ ФРОНТЕНДА
# ===========================================
NEXT_PUBLIC_API_URL=https://unl-computers.duckdns.org/api

# ===========================================
# ИНСТРУКЦИИ ПО НАСТРОЙКЕ:
# ===========================================
# 1. Сгенерируйте безопасный JWT_SECRET_KEY: openssl rand -hex 32
# 2. Создайте базу данных pc_configurator в PostgreSQL
# 3. Обновите nginx.conf в проекте events (см. nginx-update-instructions.md)
# 4. Получите SSL сертификат для unl-computers.duckdns.org
# 5. Запустите деплой: ./deploy-simple.sh 